# Copy to .env and fill in. .env is gitignored and must never be committed.
# Stage 1 needs none of these — they arrive in later stages.

# Stage 5 — local model served by Ollama
FAIRY_MODEL=qwen3:4b

# Stage 5 — optional hosted fallback (free tier)
# GROQ_API_KEY=

# Stage 4 — your Brightspace/D2L calendar feed URL
# D2L_ICS_URL=
